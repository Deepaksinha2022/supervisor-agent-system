class SynthesisAgent:

    def synthesize(
        self,
        web_results,
        retrieval_results
    ):

        return {
            "web_results": web_results,
            "retrieval_results": retrieval_results
        }